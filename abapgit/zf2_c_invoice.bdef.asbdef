projection;
strict ( 2 );
use draft;

define behavior for ZF2_C_Invoice alias Invoice
{
  use create;
  use update;
  use delete;
  use action cancelInvoice;
  use action registerPayment;
  use association _Payments;
}
