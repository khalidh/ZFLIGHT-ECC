projection;
strict ( 2 );
use draft;

define behavior for ZC_Flight alias Flight
{
  use create;
  use update;
  use delete;
  use action recalculateAvailability;
  use association _Bookings { create; with draft; }
}

