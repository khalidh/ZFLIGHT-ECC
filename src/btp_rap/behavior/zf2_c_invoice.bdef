projection;
use draft;
define behavior for ZF2_C_Invoice alias Invoice
{
  use create;
  use update;
  use delete;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}
